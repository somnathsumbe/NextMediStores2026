"use client"; import {useEffect,useState} from "react"; import Link from "next/link"; import {mockService} from "@/lib/mock-service"; import {PageHeader,Status} from "@/components/ui";
export default function Products(){const [rows,setRows]=useState<any[]>([]),[q,setQ]=useState("");useEffect(()=>setRows(mockService.get("products")),[]);const f=rows.filter(x=>Object.values(x).join(" ").toLowerCase().includes(q.toLowerCase()));return <div className="page"><PageHeader title="Products" subtitle="Medicine catalogue, batches, pricing and inventory" action="Add Product" href="/products/add"/><div className="card table-card"><div className="p-3 border-bottom d-flex justify-content-between"><div className="search" style={{maxWidth:420}}><i className="bi bi-search"></i><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search product, brand, batch..."/></div><span className="muted">{f.length} products</span></div><div className="table-responsive"><table className="table mb-0"><thead><tr><th>Code</th><th>Medicine</th><th>Brand</th><th>Category</th><th>Batch</th><th>Expiry</th><th>Stock</th><th>Price</th><th>Status</th></tr></thead><tbody>{f.map(x=><tr key={x.id}><td className="fw-semibold">{x.code}</td><td>{x.name}</td><td>{x.brand}</td><td>{x.category}</td><td>{x.batch}</td><td>{x.expiry}</td><td className={x.stock<=x.minStock?"text-danger fw-bold":""}>{x.stock}</td><td>₹{x.salePrice}</td><td>{x.stock<=x.minStock?<Status value="Low Stock"/>:<Status value="In Stock"/>}</td></tr>)}</tbody></table></div></div></div>}