"use client";

import { FormEvent, useEffect, useMemo, useState } from "react";
import { PageHeader } from "@/components/ui";
import { partyService } from "@/lib/party-service";
import { exportPartyExcel, exportPartyPdf } from "@/lib/party-export";
import type { CustomerType, Party } from "@/types/party";

type LicenceStatus = "Valid" | "Expiring Soon" | "Expired" | "Not Available";
type StatusFilter = "All" | "Active" | "Inactive";
type GstnFilter = "ALL GSTN" | "Registered" | "Not Registered";
type FormState = Omit<Party, "id" | "closingBalance" | "outstandingBalance">;
const PAGE_SIZE_OPTIONS = [10, 25, 50, 100];
const emptyForm: FormState = { firmName: "", ownerName: "", pharmacistName: "", customerType: "Dealer", active: true, email: "", phone: "", alternatePhone: "", address: "", city: "", state: "Maharashtra", pincode: "", drugLicenceNumber: "", drugLicenceExpiry: "", foodLicenceNo: "", registeredGSTN: false, gstnNumber: "", scheme: "", discount: 0, paymentTerms: "Cash", creditLimit: 0, openingBalance: 0, openingBalanceType: "Debit", creditLocked: false, contactPerson: "", whatsappNumber: "", notes: "" };

function licenceStatus(expiry: string): LicenceStatus { if (!expiry) return "Not Available"; const days = Math.ceil((new Date(`${expiry}T23:59:59`).getTime() - new Date().setHours(0, 0, 0, 0)) / 86400000); return days < 0 ? "Expired" : days <= 30 ? "Expiring Soon" : "Valid"; }
function badgeClass(status: LicenceStatus) { return status === "Expired" ? "badge-danger" : status === "Expiring Soon" ? "badge-warning" : status === "Valid" ? "badge-success" : "badge-secondary"; }

export default function Parties() {
  const [records, setRecords] = useState<Party[]>([]);
  const [form, setForm] = useState<FormState>(emptyForm);
  const [editingId, setEditingId] = useState<number | null>(null);
  const [search, setSearch] = useState("");
  const [typeFilter, setTypeFilter] = useState("");
  const [statusFilter, setStatusFilter] = useState<StatusFilter>("All");
  const [licenceFilter, setLicenceFilter] = useState<LicenceStatus | "All">("All");
  const [gstnFilter, setGstnFilter] = useState<GstnFilter>("ALL GSTN");
  const [cityFilter, setCityFilter] = useState("");
  const [pageSize, setPageSize] = useState(10);
  const [page, setPage] = useState(1);
  const [toast, setToast] = useState<{ type: "success" | "error"; message: string } | null>(null);
  const [viewing, setViewing] = useState<Party | null>(null);
  const [deleting, setDeleting] = useState<Party | null>(null);

  const refresh = () => setRecords(partyService.list());
  useEffect(refresh, []);
  const cities = partyService.cities();
  const states = partyService.states();
  const schemes = partyService.schemes();
  const today = new Date().toISOString().slice(0, 10);
  const filtered = useMemo(() => {
    const query = search.trim().toLowerCase();
    return records.filter((party) => {
      const text = [party.firmName, party.ownerName, party.pharmacistName, party.phone, party.gstnNumber, party.city].join(" ").toLowerCase();
      const licence = licenceStatus(party.drugLicenceExpiry);
      return (!query || text.includes(query)) && (!typeFilter || party.customerType === typeFilter) && (statusFilter === "All" || (party.active ? "Active" : "Inactive") === statusFilter) && (licenceFilter === "All" || licence === licenceFilter) && (gstnFilter === "ALL GSTN" || (party.registeredGSTN ? "Registered" : "Not Registered") === gstnFilter) && (!cityFilter || party.city === cityFilter);
    });
  }, [records, search, typeFilter, statusFilter, licenceFilter, gstnFilter, cityFilter]);
  const pages = Math.max(1, Math.ceil(filtered.length / pageSize));
  const rows = filtered.slice((page - 1) * pageSize, page * pageSize);
  useEffect(() => setPage(1), [search, typeFilter, statusFilter, licenceFilter, gstnFilter, cityFilter, pageSize]);
  useEffect(() => setPage((current) => Math.min(current, pages)), [pages]);

  function notify(type: "success" | "error", message: string) { setToast({ type, message }); window.setTimeout(() => setToast(null), 3200); }
  function update<K extends keyof FormState>(field: K, value: FormState[K]) { setForm((current) => ({ ...current, [field]: field === "gstnNumber" ? String(value).toUpperCase() : value })); }
  function reset() { setForm(emptyForm); setEditingId(null); }
  function clearFilters() { setSearch(""); setTypeFilter(""); setStatusFilter("All"); setLicenceFilter("All"); setGstnFilter("ALL GSTN"); setCityFilter(""); }
  function validation() {
    const email = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email);
    const phone = /^[6-9]\d{9}$/.test(form.phone);
    const alternate = !form.alternatePhone || /^\d{10}$/.test(form.alternatePhone);
    const whatsapp = !form.whatsappNumber || /^\d{10}$/.test(form.whatsappNumber);
    const gstn = !form.registeredGSTN || /^\d{2}[A-Z]{5}\d{4}[A-Z][1-9A-Z]Z[0-9A-Z]$/.test(form.gstnNumber);
    const pin = /^[1-9]\d{5}$/.test(form.pincode);
    return email && phone && alternate && whatsapp && gstn && pin && !!form.firmName.trim() && !!form.ownerName.trim() && !!form.pharmacistName.trim() && !!form.address.trim() && !!form.city && !!form.state && !!form.drugLicenceNumber.trim() && !!form.drugLicenceExpiry && !!form.foodLicenceNo.trim() && form.creditLimit >= 0 && form.openingBalance >= 0;
  }
  function submit(event: FormEvent) {
    event.preventDefault();
    if (!validation()) { notify("error", "Please complete all required fields with valid values."); return; }
    const existing = records.find((party) => party.id === editingId);
    const payload: FormState = { ...form, gstnNumber: form.registeredGSTN ? form.gstnNumber.trim().toUpperCase() : "" };
    try {
      if (editingId === null) { partyService.create({ ...payload, closingBalance: payload.openingBalance, outstandingBalance: payload.openingBalance }); notify("success", "Party added successfully."); }
      else { partyService.update(editingId, { ...payload, closingBalance: existing?.closingBalance ?? payload.openingBalance, outstandingBalance: existing?.outstandingBalance ?? payload.openingBalance }); notify("success", "Party updated successfully."); }
      refresh(); reset();
    } catch (error) { notify("error", error instanceof Error ? error.message : "Unable to save party."); }
  }
  function edit(party: Party) { const { id, closingBalance, outstandingBalance, ...values } = party; void id; void closingBalance; void outstandingBalance; setForm(values); setEditingId(party.id); window.scrollTo({ top: 0, behavior: "smooth" }); }
  function remove() { if (!deleting) return; try { partyService.delete(deleting.id); setDeleting(null); refresh(); notify("success", "Party deleted successfully."); } catch (error) { notify("error", error instanceof Error ? error.message : "Unable to delete party."); } }
  function toggle(party: Party) { partyService.toggleStatus(party.id); refresh(); notify("success", `${party.firmName} marked ${party.active ? "Inactive" : "Active"}.`); }
  function toggleCreditLock(party: Party) { partyService.toggleCreditLock(party.id); refresh(); notify("success", `${party.firmName} credit is now ${party.creditLocked ? "Unlocked" : "Locked"}.`); }
  const field = (name: keyof FormState, label: string, type = "text", required = false, disabled = false) => <div className="col-md-4"><label className="form-label" htmlFor={name}>{label}{required ? " *" : ""}</label><input id={name} className="form-control" type={type} value={String(form[name])} required={required} disabled={disabled} onChange={(event) => update(name, type === "number" ? Number(event.target.value) : event.target.value as FormState[typeof name])} /></div>;

  return <div className="page party-page"><PageHeader title="Party Details" subtitle="Manage pharmacies, hospitals, distributors and suppliers" />{toast && <div className={`alert alert-${toast.type === "success" ? "success" : "danger"}`} role="status">{toast.message}</div>}
    <form onSubmit={submit} noValidate><section className="card p-4 mb-3"><h2 className="h5 mb-3">Basic Information</h2><div className="row g-3">{field("firmName", "Firm Name", "text", true)}{field("ownerName", "Owner Name", "text", true)}{field("pharmacistName", "Pharmacist Name", "text", true)}<div className="col-md-4"><label className="form-label" htmlFor="customerType">Customer Type *</label><select id="customerType" className="form-select" value={form.customerType} onChange={(e) => update("customerType", e.target.value as CustomerType)}><option>Dealer</option><option>Retailer</option><option>Other</option></select></div><div className="col-md-4"><label className="form-label d-block">Active Party *</label><div className="form-check form-check-inline"><input id="active-yes" className="form-check-input" type="radio" checked={form.active} onChange={() => update("active", true)} /><label className="form-check-label" htmlFor="active-yes">Yes</label></div><div className="form-check form-check-inline"><input id="active-no" className="form-check-input" type="radio" checked={!form.active} onChange={() => update("active", false)} /><label className="form-check-label" htmlFor="active-no">No</label></div></div>{field("email", "Email ID", "email", true)}{field("phone", "Phone Number", "tel", true)}{field("alternatePhone", "Alternate Phone")}{field("address", "Address", "text", true)}<div className="col-md-4"><label className="form-label" htmlFor="city">City *</label><select id="city" className="form-select" value={form.city} onChange={(e) => update("city", e.target.value)} required><option value="">Select City</option>{cities.map((city) => <option key={city}>{city}</option>)}</select></div><div className="col-md-4"><label className="form-label" htmlFor="state">State *</label><select id="state" className="form-select" value={form.state} onChange={(e) => update("state", e.target.value)} required>{states.map((state) => <option key={state}>{state}</option>)}</select></div>{field("pincode", "Pincode", "text", true)}</div></section>
      <section className="card p-4 mb-3"><h2 className="h5 mb-3">License &amp; Tax Details</h2><div className="row g-3">{field("drugLicenceNumber", "Drug Licence Number", "text", true)}<div className="col-md-4"><label className="form-label" htmlFor="drugLicenceExpiry">Drug Licence Expiry *</label><input id="drugLicenceExpiry" className="form-control" type="date" min={today} value={form.drugLicenceExpiry} required onChange={(event) => update("drugLicenceExpiry", event.target.value)} /></div>{field("foodLicenceNo", "Food Licence No", "text", true)}<div className="col-md-4 d-flex align-items-end"><div className="form-check mb-2"><input id="registeredGSTN" className="form-check-input" type="checkbox" checked={form.registeredGSTN} onChange={(event) => setForm((current) => ({ ...current, registeredGSTN: event.target.checked, gstnNumber: event.target.checked ? current.gstnNumber : "" }))} /><label className="form-check-label" htmlFor="registeredGSTN">Registered GSTN</label></div></div>{field("gstnNumber", "GSTN Number", "text", form.registeredGSTN, !form.registeredGSTN)}</div></section>
      <section className="card p-4 mb-3"><h2 className="h5 mb-3">Commercial Details</h2><div className="row g-3">{field("scheme", "Scheme")}{field("discount", "Discount", "number")}{field("paymentTerms", "Payment Terms")}{field("creditLimit", "Credit Limit", "number", true)}{field("openingBalance", "Opening Balance", "number", true)}<div className="col-md-4"><label className="form-label" htmlFor="openingBalanceType">Balance Type</label><select id="openingBalanceType" className="form-select" value={form.openingBalanceType} onChange={(e) => update("openingBalanceType", e.target.value as FormState["openingBalanceType"])}><option>Debit</option><option>Credit</option></select></div></div></section>
      <section className="card p-4 mb-3"><h2 className="h5 mb-3">Additional Information</h2><div className="row g-3">{field("contactPerson", "Contact Person")}{field("whatsappNumber", "WhatsApp Number")}<div className="col-12"><label className="form-label" htmlFor="notes">Notes</label><textarea id="notes" className="form-control" rows={2} value={form.notes} onChange={(e) => update("notes", e.target.value)} /></div></div></section><div className="d-flex justify-content-end gap-2 mb-4"><button type="button" className="btn btn-light" onClick={reset}>Reset</button><button type="submit" className="btn btn-primary">{editingId ? "Update Party" : "Add Party"}</button></div></form>
    <section className="card table-card"><div className="p-3 border-bottom"><div className="d-flex flex-wrap justify-content-between gap-2 mb-3"><div><h2 className="h5 mb-1">Party Details List</h2><span className="muted">{filtered.length} of {records.length} records</span></div><div className="d-flex flex-wrap gap-2"><button type="button" className="btn btn-outline-success btn-sm" onClick={() => void exportPartyExcel(filtered)}><i className="bi bi-file-earmark-excel me-1" />Download Excel</button><button type="button" className="btn btn-outline-danger btn-sm" onClick={() => void exportPartyPdf(filtered)}><i className="bi bi-file-earmark-pdf me-1" />Download PDF</button></div></div><div className="d-flex flex-wrap gap-2"><input className="form-control flex-grow-1" style={{ maxWidth: 420 }} placeholder="Search firm, owner, pharmacist, phone, GSTN, city..." value={search} onChange={(e) => setSearch(e.target.value)} /><select className="form-select" style={{ maxWidth: 140 }} value={typeFilter} onChange={(e) => setTypeFilter(e.target.value)}><option value="">All Types</option><option>Dealer</option><option>Retailer</option><option>Other</option></select><select className="form-select" style={{ maxWidth: 140 }} value={statusFilter} onChange={(e) => setStatusFilter(e.target.value as StatusFilter)}><option>All</option><option>Active</option><option>Inactive</option></select><select className="form-select" style={{ maxWidth: 160 }} value={licenceFilter} onChange={(e) => setLicenceFilter(e.target.value as LicenceStatus | "All")}><option>All</option><option>Valid</option><option>Expiring Soon</option><option>Expired</option></select><select className="form-select" aria-label="GSTN Status" style={{ maxWidth: 150 }} value={gstnFilter} onChange={(e) => setGstnFilter(e.target.value as GstnFilter)}><option>ALL GSTN</option><option>Registered</option><option>Not Registered</option></select><select className="form-select" style={{ maxWidth: 140 }} value={cityFilter} onChange={(e) => setCityFilter(e.target.value)}><option value="">All Cities</option>{cities.map((city) => <option key={city}>{city}</option>)}</select><button type="button" className="btn btn-light" onClick={clearFilters}>Clear Filters</button></div></div><div className="table-responsive"><table className="table align-middle mb-0"><thead><tr><th>Firm Name</th><th>Owner Name</th><th>Phone</th><th>GSTN</th><th>Type</th><th>City</th><th>Licence Status</th><th>Party Status</th><th>Actions</th></tr></thead><tbody>{rows.length ? rows.map((party) => { const licence = licenceStatus(party.drugLicenceExpiry); return <tr key={party.id}><td>{party.firmName}</td><td>{party.ownerName}</td><td>{party.phone}</td><td>{party.registeredGSTN ? party.gstnNumber : "Not Registered"}</td><td>{party.customerType}</td><td>{party.city}</td><td><span className={`badge-soft ${badgeClass(licence)}`}>{licence}</span></td><td><span className={`badge-soft ${party.active ? "badge-success" : "badge-danger"}`}>{party.active ? "Active" : "Inactive"}</span></td><td><div className="d-flex flex-wrap gap-1"><button type="button" className="btn btn-sm btn-light" title="View" aria-label={`View ${party.firmName}`} onClick={() => setViewing(party)}><i className="bi bi-eye" /></button><button type="button" className="btn btn-sm btn-light" title="Edit" aria-label={`Edit ${party.firmName}`} onClick={() => edit(party)}><i className="bi bi-pencil" /></button><button type="button" className="btn btn-sm btn-light" title={party.active ? "Deactivate" : "Activate"} aria-label={`${party.active ? "Deactivate" : "Activate"} ${party.firmName}`} onClick={() => toggle(party)}><i className={`bi ${party.active ? "bi-toggle-on text-success" : "bi-toggle-off text-muted"}`} /></button><button type="button" className="btn btn-sm btn-light" title={party.creditLocked ? "Unlock Credit" : "Lock Credit"} aria-label={`${party.creditLocked ? "Unlock" : "Lock"} credit for ${party.firmName}`} onClick={() => toggleCreditLock(party)}><i className={`bi ${party.creditLocked ? "bi-lock-fill text-warning" : "bi-unlock text-secondary"}`} /></button><button type="button" className="btn btn-sm btn-outline-danger" title="Delete" aria-label={`Delete ${party.firmName}`} onClick={() => setDeleting(party)}><i className="bi bi-trash" /></button></div></td></tr>; }) : <tr><td colSpan={9} className="text-center py-5 muted">No party records found.</td></tr>}</tbody></table></div>{filtered.length > 0 && <div className="d-flex flex-wrap justify-content-between align-items-center gap-2 p-3 border-top"><span className="muted small">Showing {(page - 1) * pageSize + 1}-{Math.min(page * pageSize, filtered.length)} of {filtered.length}</span><div className="d-flex gap-2"><select className="form-select form-select-sm" style={{ width: 90 }} value={pageSize} onChange={(e) => setPageSize(Number(e.target.value))}>{PAGE_SIZE_OPTIONS.map((size) => <option key={size}>{size}</option>)}</select><button type="button" className="btn btn-sm btn-outline-secondary" disabled={page === 1} onClick={() => setPage((current) => current - 1)}>Previous</button><span className="small align-self-center">{page} / {pages}</span><button type="button" className="btn btn-sm btn-outline-secondary" disabled={page === pages} onClick={() => setPage((current) => current + 1)}>Next</button></div></div>}</section>
    {viewing && <div className="bank-modal-backdrop" onClick={() => setViewing(null)} role="presentation"><section className="card bank-view-modal" role="dialog" aria-modal="true" onClick={(e) => e.stopPropagation()}><div className="d-flex justify-content-between"><div><p className="eyebrow mb-1">Party account</p><h2 className="h4">{viewing.firmName}</h2></div><button className="btn-close" onClick={() => setViewing(null)} aria-label="Close" /></div><div className="row g-3 small mt-2"><div className="col-6"><span className="muted">Owner Name</span><strong className="d-block">{viewing.ownerName}</strong></div><div className="col-6"><span className="muted">Customer Type</span><strong className="d-block">{viewing.customerType}</strong></div><div className="col-6"><span className="muted">Phone</span><strong className="d-block">{viewing.phone}</strong></div><div className="col-6"><span className="muted">Registered GSTN</span><strong className="d-block">{viewing.registeredGSTN ? viewing.gstnNumber : "No"}</strong></div><div className="col-12"><span className="muted">Address</span><strong className="d-block">{viewing.address}, {viewing.city}, {viewing.state} - {viewing.pincode}</strong></div><div className="col-6"><span className="muted">Drug Licence</span><strong className="d-block">{viewing.drugLicenceNumber}</strong></div><div className="col-6"><span className="muted">GSTN Number</span><strong className="d-block">{viewing.registeredGSTN ? viewing.gstnNumber : "-"}</strong></div><div className="col-6"><span className="muted">Credit Limit</span><strong className="d-block">₹{viewing.creditLimit.toLocaleString("en-IN")}</strong></div><div className="col-6"><span className="muted">Outstanding</span><strong className="d-block">₹{viewing.outstandingBalance.toLocaleString("en-IN")}</strong></div></div></section></div>}
    {deleting && <div className="bank-modal-backdrop" onClick={() => setDeleting(null)} role="presentation"><section className="card bank-view-modal" role="alertdialog" aria-modal="true" onClick={(e) => e.stopPropagation()}><h2 className="h5">Delete Party?</h2><p className="muted">Delete {deleting.firmName}? This action cannot be undone.</p><div className="text-end"><button className="btn btn-light me-2" onClick={() => setDeleting(null)}>Cancel</button><button className="btn btn-danger" onClick={remove}>Delete</button></div></section></div>}
  </div>;
}
